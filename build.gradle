plugins {
    id 'io.github.gradle-nexus.publish-plugin' version '1.1.0'
}

subprojects {
    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
        mavenLocal()
    }
}
